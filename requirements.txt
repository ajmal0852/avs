google-genai==2.8.0
streamlit==1.58.0
pydantic==2.13.4
python-dotenv==1.2.2
python-docx==1.2.0
pypdf==6.13.1

# Notes:
# - These are the top-level runtime dependencies used by the project.
# - The google-genai package will bring in httpx and other transitive deps.
# - Adjust versions if your deployment environment requires different pins.
